# Salt master configuration (uploaded by the Vagrant salt provisioner).
# The compute minion's key is accepted automatically (assignment requirement).
interface: 0.0.0.0
auto_accept: True

file_roots:
  base:
    - /vagrant/salt/states

pillar_roots:
  base:
    - /vagrant/salt/pillar
